Your Event is Tomorrow!

<%= @event.title -%> will begin at <%= @event.session_start.strftime("%l:%M %p %Z") %> on <%= @event.session_start.strftime("%B %e, %Y") -%> and last <%= @event.session_length %> minutes long.

Get more information and the URL to join at <%= event_url(@event.id) -%>.

<% if @event.registration_description.blank? %>
Event Description: <%= Event.html_to_pretty_text(@event.description.gsub! "\r", "") %>
<% else %>
Event Description: <%= Event.html_to_pretty_text(@event.registration_description.gsub! "\r", "") %>
<% end %>

More Information:
This email was sent to you at <%= @registration.email %> because you registered for an event at learn.extension.org

You received this email because you registered for this event at learn.extension.org.

Get the URL at <%= event_url(@event.id) -%> and join the event at <%= @event.session_start.strftime("%l:%M %p %Z") %> on <%= @event.session_start.strftime("%B %e, %Y") -%>.