<ul class="nav nav-tabs">
	<%= nav_item(data_overview_path, 'Overall') -%>
	<%= nav_item(data_events_path, 'Events') -%>
	<%= nav_item(data_zoom_webinars_path, 'Zoom Webinars') -%>

  <%= nav_item(data_presenters_path, 'Presenters') if current_learner.is_admin? -%>
  <%= nav_item(data_activity_path, 'All Activity') if current_learner.is_admin? -%>
  <%= nav_item(data_blocked_activity_path, 'Blocked Learners') if current_learner.is_admin? %>
</ul>
