"Event Statistics for <%= @start_date %> - <%= @end_date %>"
"Note: Event Date/Time is relative to your specified timezone - <%= current_learner.time_zone.html_safe %>, if sharing this csv with others in other timezones, please be mindful of this"

Date/Time,Canceled,Expired,Presenters,Title,Tags,URL,Recording,Length,Bookmarked,Attended,Watched,Voters,Commentators,
<%- for event in @events -%>
"<%= event.session_start.strftime("%Y-%m-%d %H:%M:%S") %>",<%= (event.is_canceled?) ? "canceled" : "" %>,<%= (event.is_expired?) ? "expired" : "" %>,"<%= event.presenters.collect{|learner| learner.name}.join(' ') %>","<%= event.title %>","<%= event.tags.collect{|tag| tag.name}.join(' ') %>","<%= event_url(event) %>","<%= (event.has_recording?) ? event.recording : "n/a" %>",<%= event.session_length %>,<%= event.bookmarked.count %>,<%= event.attendees.count %>,<%= event.watched.count %>,<%= event.raters.count %>,<%= event.commentators.count %>
<%- end -%>
